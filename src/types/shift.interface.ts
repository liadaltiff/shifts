export interface Shift {
  _id: string;
  dateProp: Date;
  shiftName: string;
  shiftPerson: string;
  shiftPersonId: string;
  startTimeValue: string;
  endTimeValue: string;
  // isTradable: boolean;
}
